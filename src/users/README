User모듈 클린 아키텍처 적용

외부 레이어   --->    내부 레이어
infra   ->    interface   ->    application   ->    domain

1. 인프라스트럭처 레이어(infra): 외부에서 가져온 라이브러리를 사용하는 레이어
2. 인터페이스 레이어(interface): 우리 서비스가 제공하는 인터페이스가 구현되는 레이어
3. 어플리케이션 레이어(application): 비즈니스 로직이 구현되는 레이어
    
4. 도메인 레이어(domain): 어플리케이션의 핵심 도메인을 구현하는 레이어
    도메인 객체와 그 도메인 객체의 상태변화에 따라 발생되는 이벤트가 존재


모든 레이어에서 사용할 공통 컴포넌트
common


